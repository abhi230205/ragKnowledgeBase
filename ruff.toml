# Lint/format config for the api package. `ruff format` runs on save via a
# Claude Code hook; `python -m ruff check api` can be run manually / in CI.
line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]  # don't fight the occasional long string/URL
