# Drop your Google service-account JSON here (e.g. service_account.json) for the
# optional file-based auth path. Everything in this directory except this file is
# gitignored — credentials are never committed. The Settings UI upload path stores
# creds in SQLite on the app-data volume instead.
