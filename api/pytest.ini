[pytest]
# Run pytest from the api/ directory so top-level packages (routes, db, ingestion,
# embeddings, vectorstore, llm) import the same way they do under uvicorn.
pythonpath = .
testpaths = tests
addopts = -v
